<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="comp3350.srsys.CheckMessageActivity" >

    <ListView
        android:id="@+id/browseInventoryListView"
        android:layout_width="match_parent"
        android:layout_height="1.25in"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" >
    </ListView>

    <EditText
        android:id="@+id/respondMessageTextField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/browseInventoryListView"
        android:layout_below="@+id/browseInventoryListView"
        android:layout_marginTop="48dp"
        android:ems="10"
        android:hint="@string/repond_message_text_field"
        android:inputType="textMultiLine" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/sendMessageButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/respondMessageTextField"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="36dp"
        android:text="@string/send_message_button" />

</RelativeLayout>
